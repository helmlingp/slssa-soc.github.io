lsi: false
safe: true
source: /docs
domain: slssa-soc.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://slssa-soc.github.io  # the base hostname and protocol for your site, e.g. http://example.com
incremental: false
highlighter: rouge
gist:
  noscript: false
kramdown:
  math_engine: mathjax
  syntax_highlighter: rouge